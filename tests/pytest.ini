[pytest]
markers = 
    algorithms: marks tests as test requiring the deploymed test algorithms algorithms (deselect with '-m "not algorithms"')
filterwarnings =
    ignore:datetime.datetime.utcnow:DeprecationWarning:botocore
    ignore::UserWarning:t3d_server\.algorithm_utils\.AlgorithmConfigSchema